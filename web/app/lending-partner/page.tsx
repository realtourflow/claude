"use client";

// #307 — the lending_partner landing route. The honest "not available yet"
// placeholder is rendered by LendingPartnerLayout (dispatched inside AppLayout),
// which deliberately does not render this page's output, so this page is an
// intentionally empty holder that just makes /lending-partner resolve.
export default function LendingPartnerPage() {
  return null;
}
